export const setWatched = (payload) => ({
    type: "SET_WATCHED",
    payload,

});

export const deleteWatched = (payload)=>({
    type: "DELETE_WATCHED",
    payload,
});

export const setSection = (payload) =>({
    type: "SET_SECTION",
    payload,
});